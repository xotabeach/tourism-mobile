import 'package:flutter/material.dart';

/// Raw brand palette. Prefer [Theme.of] / semantic theme extensions in UI.
abstract final class AppColors {
  static const Color seed = Color(0xFF1B6B93);
  static const Color sea = Color(0xFF1B6B93);
  static const Color coastline = Color(0xFF0E4D6B);
  static const Color sand = Color(0xFFF3E6D4);
  static const Color cliff = Color(0xFF5C4A3A);
  static const Color mist = Color(0xFFE8EEF2);
}
